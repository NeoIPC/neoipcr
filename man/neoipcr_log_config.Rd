% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log.R
\name{neoipcr_log_config}
\alias{neoipcr_log_config}
\title{Configure neoipcr's log verbosity}
\usage{
neoipcr_log_config(verbosity = NULL)
}
\arguments{
\item{verbosity}{One of \code{"quiet"}, \code{"normal"}, \code{"verbose"}, \code{"debug"}
(mapped to logger thresholds \code{WARN}, \code{INFO}, \code{DEBUG}, \code{TRACE}). When
\code{NULL} (the default) the level is read from the \code{NEOIPC_LOG_LEVEL}
environment variable, falling back to \code{"normal"}. neoipcr's per-request
query trace is emitted at \code{DEBUG}, so \code{"verbose"} or \code{"debug"} reveals it.}
}
\value{
The applied logger threshold, invisibly.
}
\description{
Sets the log threshold for neoipcr's \code{"neoipcr"} logger namespace from a
verbosity level, controlling how much of the DHIS2 query trace and import
progress neoipcr emits. neoipcr logs through the \pkg{logger} package; this
is a thin convenience over \code{\link[logger:log_threshold]{logger::log_threshold()}} using neoipcr's
verbosity vocabulary.
}
\details{
neoipcr is silent by default: it logs only at \code{DEBUG}/\code{TRACE}, and the
global logger threshold defaults to \code{INFO}. neoipcr's \code{"neoipcr"} namespace
threshold is set from \code{NEOIPC_LOG_LEVEL} in two places: once at package load
(\code{.onLoad}) and whenever \code{neoipcr_log_config()} is called with no argument. A
bare \code{\link[=import_dhis2]{import_dhis2()}} therefore honours \code{NEOIPC_LOG_LEVEL} only when the
variable was set \emph{before} the package was loaded; to pick up a change made in
an already-loaded session, call \code{neoipcr_log_config()}. An explicit
\code{neoipcr_log_config(verbosity=)} call sets the namespace threshold; otherwise
the namespace inherits the global logger threshold.
}
\examples{
neoipcr_log_config("debug")  # reveal the DHIS2 query trace
}
