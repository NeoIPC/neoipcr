% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci.R
\name{bootstrap_quantile_ci}
\alias{bootstrap_quantile_ci}
\title{Two-level parametric bootstrap CI for benchmark quartiles}
\usage{
bootstrap_quantile_ci(
  events,
  exposure,
  type = c("poisson", "binomial"),
  multiplier = 1,
  B = 2000,
  conf.level = 0.95,
  seed = 42
)
}
\arguments{
\item{events}{Integer vector. Observed event counts per department. Must be
non-negative.}

\item{exposure}{Numeric vector. Denominators per department (patient-days,
device-days, total infections, etc.). Must be strictly positive. Same
length as \code{events}. Departments with zero denominators must be excluded
upstream.}

\item{type}{Character. Rate type: \code{"poisson"} for count-over-exposure
densities, \code{"binomial"} for true proportions. Determines the resampling
distribution.}

\item{multiplier}{Numeric. Scaling factor applied to bootstrap rates before
quantile computation. Default 1. Use 1000 for incidence densities, 100
for utilisation densities.}

\item{B}{Integer. Number of bootstrap iterations. Default 2000.}

\item{conf.level}{Numeric. Confidence level. Default 0.95.}

\item{seed}{Numeric or NULL. RNG seed for reproducibility. Default 42.
Set to NULL to skip seeding (non-deterministic).}
}
\value{
A one-row tibble with six columns:
\code{q1_ci_lower}, \code{q1_ci_upper}, \code{q2_ci_lower}, \code{q2_ci_upper},
\code{q3_ci_lower}, \code{q3_ci_upper}.
}
\description{
Computes confidence intervals for the departmental quartiles (Q1, Q2, Q3)
of a surveillance metric. This is the only CI method in the pipeline that
uses simulation, because no closed-form solution exists for the two-layer
uncertainty structure: within-department sampling noise and cross-department
quantile estimation uncertainty.
}
\details{
Uses Jeffreys non-informative priors to propagate parameter uncertainty even
for zero-count departments. For Poisson rates, the prior is
\code{Gamma(0.5, 0)}; for binomial proportions, \code{Beta(0.5, 0.5)}. This ensures
honest uncertainty propagation where naive resampling from \code{Poisson(0)}
would always return 0.
}
\examples{
# Poisson: 6 departments with infection counts over patient-days
bootstrap_quantile_ci(
  events = c(5, 0, 12, 3, 8, 20),
  exposure = c(1000, 800, 1200, 600, 900, 1500),
  type = "poisson", multiplier = 1000)

# Binomial: detection proportions
bootstrap_quantile_ci(
  events = c(15, 8, 22, 5, 18, 30),
  exposure = c(20, 10, 25, 8, 20, 35),
  type = "binomial", multiplier = 100)

}
