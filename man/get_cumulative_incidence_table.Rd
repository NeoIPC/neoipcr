% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-tables.R
\name{get_cumulative_incidence_table}
\alias{get_cumulative_incidence_table}
\title{Get the table with the cumulative incidence of infection per admission window}
\usage{
get_cumulative_incidence_table(
  x,
  windows,
  event_types = c("bsi", "hap"),
  unit = c("patients", "enrollments"),
  conf.level = 0.95
)
}
\arguments{
\item{x}{A \code{neoipcr_ds} object imported with \code{include_enrollment = "full"}
and \code{include_event = "full"}, and with \code{include_patient} and
\code{include_department} not \code{"no"}: the admission date, the event date and
the department link are what this computation reads. The admission date
is the enrollment date (\code{enrolledAt}): in the NeoIPC data model the
enrollment is the admission, and an admission event dated differently is
a validation error (rule 3) that a validation-clean import does not
contain. Under \code{include_invalid_patients = TRUE} such an admission is
placed by its enrollment date.}

\item{windows}{A data frame with one row per window: \code{department_key}
(integer, never \code{NA}), \code{window} (a label such as \code{"baseline"}), and the
inclusive \code{start} and \code{end} dates (\code{Date}). Windows may overlap; an
admission is counted in every window it falls into. Grouping is ignored
and further columns are dropped. An admission without an admission date,
or an infection without an event date (possible under
\code{include_incomplete}), falls into no window.}

\item{event_types}{The event types that count as an infection, a subset of
\code{"bsi"}, \code{"nec"}, \code{"hap"} and \code{"ssi"}.}

\item{unit}{Whether \code{n} and \code{n_infected}, and with them the proportion,
count distinct \code{"patients"} (the default: a patient admitted twice in one
window counts once) or \code{"enrollments"} (admissions).}

\item{conf.level}{Confidence level of the Wilson interval. Default 0.95.}
}
\value{
A tibble with one row per row of \code{windows}: \code{department_key},
\code{window}, \code{start}, \code{end}, the four counts \code{n_patients}, \code{n_enrollments},
\code{n_infected_patients} and \code{n_infected_enrollments}, then \code{n} and
\code{n_infected} for the chosen \code{unit}, \code{proportion} (per 100; \code{NA} when \code{n}
is 0) and its \code{ci_lower} and \code{ci_upper}.
}
\description{
The cumulative incidence is the share of the patients (or admissions)
admitted to a department within a calendar window who acquired at least one
infection of the given types within that same window. It complements the
incidence density tables, which relate infections to patient days rather
than to the admitted cohort, and is the measure a trial asks for when its
study periods are calendar windows per site.
}
\details{
The default \code{event_types}, primary sepsis/BSI plus pneumonia, is the
"severe infection" composite (\code{si}) that
\code{\link[=get_incidence_density_rate_table]{get_incidence_density_rate_table()}} reports. The confidence interval is the
Wilson score interval, the house method for a proportion of discrete
entities.
}
